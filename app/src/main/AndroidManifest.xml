<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projektlodowka">

    <application
        android:allowBackup="true"
        android:icon="@drawable/efrigde"
        android:label="@string/app_name"
        android:roundIcon="@drawable/efrigde"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".produkty_list_adapter"></activity>
        <activity android:name=".ustawienia_activity" />
        <activity android:name=".historia_activity" />
        <activity android:name=".przepisy_activity" />
        <activity android:name=".produkty_activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>